{
  "private": true,
  "devDependencies": {
    "mocha": "^3.0.2",
    "chai": "^3.5.0",
    "karma": "^1.3.0",
    "karma-mocha": "^1.1.1",
    "karma-chrome-launcher": "^2.0.0",
    "karma-chai": "^0.1.0",
    "watch": "^0.19.2",
    "mkdirp": "0.5.x"
  },
  "scripts": {
    "test": "karma start --single-run",
    "prewatch": "mkdirp dist",
    "watch": "watch 'cat js/*.js > dist/app.js' js --interval=0.01",
    "start": "npm install && npm run watch"
  }
}
